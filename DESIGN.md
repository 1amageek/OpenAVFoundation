# OpenAVFoundation Design

## Status

This document is the normative design for the package. The package is currently
at **basic capture-graph smoke stage**: discovery feeds a one-device-input /
one-video-output graph, configuration commits atomically, and session start
drives the Driver handle through snapshot, preferred configuration, stream
creation, and sample delivery. Multi-output fan-out and bounded backpressure
remain unimplemented. Import or build success is not evidence of working capture.

## Apple API review

The initial capture architecture was derived from Apple's public documentation,
read with `remark` on 2026-07-24:

- [AVCaptureDevice](https://developer.apple.com/documentation/avfoundation/avcapturedevice)
- [AVCaptureDevice.DiscoverySession](https://developer.apple.com/documentation/avfoundation/avcapturedevice/discoverysession)
- [AVCaptureDevice.DeviceType](https://developer.apple.com/documentation/avfoundation/avcapturedevice/devicetype-swift.struct)
- [AVCaptureDevice.Position](https://developer.apple.com/documentation/avfoundation/avcapturedevice/position-swift.enum)
- [AVMediaType](https://developer.apple.com/documentation/avfoundation/avmediatype)
- [AVAuthorizationStatus](https://developer.apple.com/documentation/avfoundation/avauthorizationstatus)
- [authorizationStatus(for:)](https://developer.apple.com/documentation/avfoundation/avcapturedevice/authorizationstatus(for:))
- [requestAccess(for:completionHandler:)](https://developer.apple.com/documentation/avfoundation/avcapturedevice/requestaccess(for:completionhandler:))
- [AVCaptureDeviceInput](https://developer.apple.com/documentation/avfoundation/avcapturedeviceinput)
- [AVCaptureInput](https://developer.apple.com/documentation/avfoundation/avcaptureinput)
- [AVCaptureOutput](https://developer.apple.com/documentation/avfoundation/avcaptureoutput)
- [AVCaptureConnection](https://developer.apple.com/documentation/avfoundation/avcaptureconnection)
- [AVCaptureSession](https://developer.apple.com/documentation/avfoundation/avcapturesession)
- [AVCaptureVideoDataOutput](https://developer.apple.com/documentation/avfoundation/avcapturevideodataoutput)
- [Setting up a capture session](https://developer.apple.com/documentation/avfoundation/setting-up-a-capture-session)

The second smoke refreshed signatures from the macOS 27 SDK symbol graph
generated by [`../scripts/extract-media-api.sh`](../scripts/extract-media-api.sh)
and the installed `AVCaptureInput.h`, `AVCaptureOutputBase.h`,
`AVCaptureSession.h`, and `AVCaptureVideoDataOutput.h` headers. The reviewed
Apple spellings include
`AVCaptureInput.Port`, `AVCaptureDeviceInput.init(device:)`,
`AVCaptureConnection.init(inputPorts:output:)`,
`captureOutput(_:didOutput:from:)`, and the session configuration/lifecycle
operations.

Apple's model is a graph. Devices are found through discovery rather than directly
constructed. Device inputs expose media ports. Connections map compatible ports
to outputs. A capture session atomically configures and runs the graph.

### Portable API differences

The shared target preserves Apple's basic type and operation names but cannot
claim every Objective-C execution convention:

- Apple configuration and lifecycle methods are synchronous and generally
  report invalid use through exceptions or notifications. OpenAVFoundation
  configuration methods use typed `throws`; Native/WASM start and stop use
  `async throws` because Driver I/O suspends.
- Embedded start and stop preserve the same names with synchronous typed throws,
  matching the owner-isolated synchronous Driver contract.
- `AVCaptureInput.Port` has stable reference identity. Native/WASM use a weak
  back-reference to their input; Embedded Swift has no weak references, so its
  owner-isolated contract requires a port not to outlive the input that vends it.
- Apple's `setSampleBufferDelegate(_:queue:)` requires Dispatch. The shared
  target instead provides `setSampleBufferDelegate(_:)`; Driver `offer` invokes
  the delegate synchronously and in Driver stream order.
- The current graph intentionally supports one `AVCaptureDeviceInput` and one
  `AVCaptureVideoDataOutput`. `canAddInput` / `canAddOutput` return `false` past
  that limit, and mutation reports a typed failure.

These differences are explicit compatibility boundaries, not silent fallbacks.

## Package boundary

### OpenAVFoundation

The user-facing compatibility product. It owns Apple-compatible capture types and
the session coordinator. Applications normally import only this product.

### OpenAVFoundationDriver dependency

The sibling package that owns the platform integration contract. Separate
packages implement it for browsers, embedded boards, Linux media stacks, vendor
camera SDKs, replay fixtures, or other sources. It is not a second capture
framework and does not expose a user-facing session graph.

No concrete driver is built into either package.

## Responsibility

OpenAVFoundation owns:

- device discovery and stable device identity;
- authorization state as reported by a platform driver;
- device capability and format representation;
- exclusive device-configuration transactions;
- capture inputs and their media ports;
- capture outputs and their delivery policies;
- input-to-output connections;
- atomic session graph configuration;
- session lifecycle, interruption, and runtime failure;
- opening each selected source once;
- fan-out from one source to multiple outputs;
- source-sequence and timestamp correlation;
- output-specific backpressure and frame-drop reporting.

OpenAVFoundation does not own:

- pixel storage implementation;
- rational time or sample-buffer representation;
- camera enumeration mechanisms such as libargus, V4L2, or browser APIs;
- codec implementations;
- gesture recognition, model selection, or Manas interpretation;
- home-automation or actuator control.

## Dependency direction

```text
OpenCoreVideo
      ▲
      ├──────────────┐
OpenCoreMedia        │
      ▲              │
      ├── OpenAVFoundationDriver ◄── Browser / Replay / V4L2 / Argus driver
      │              ▲
      └── OpenAVFoundation
```

Driver packages depend on `OpenAVFoundationDriver`; the compatibility module does
not import any concrete driver package.

## Capture graph

```text
AVCaptureDevice.DiscoverySession
                 │ obtains descriptors from installed drivers
                 ▼
          AVCaptureDevice
                 │ wrapped by
                 ▼
       AVCaptureDeviceInput
                 │ exposes ports
                 ▼
        AVCaptureConnection
                 │ routes samples
                 ▼
          AVCaptureOutput

AVCaptureSession owns configuration, connection formation, source opening,
runtime ordering, fan-out, shutdown, and failure propagation.
```

`beginConfiguration()` and `commitConfiguration()` form an atomic graph
transaction. An invalid graph is rejected without partially mutating the running
graph.

## Device-driver contract

The driver extension contract is divided by responsibility rather than camera
model.

| Contract | Driver responsibility | Framework responsibility |
|---|---|---|
| Discovery | Enumerate descriptors and capability revisions | Filter by device type, media type, and position |
| Identity | Provide stable driver-local identifiers | Namespace identifiers and preserve object identity |
| Authorization | Report/request platform access | Expose compatible authorization state |
| Open | Produce an exclusive or shared source handle | Enforce session ownership and lifecycle |
| Formats | Describe supported media streams and controls | Validate requested session configuration |
| Configuration | Apply a validated configuration transaction | Implement lock and atomicity semantics |
| Streaming | Produce leased `CMSampleBuffer` values or typed failures | Route, correlate, fan out, and apply backpressure |
| Shutdown | Stop production and release device resources | Order shutdown and finish all output streams |

Driver discovery publishes immutable descriptors. Opening a device is separate
from discovery. A driver cannot inject an already-running global stream into a
session.

Device IDs consist semantically of a driver namespace and a stable local ID.
Duplicate IDs, changing capabilities without a revision change, or a driver
disappearing while silently leaving a fake device are contract violations.

### Driver composition

- Browser applications install a browser media driver during application startup.
- Embedded applications statically compose only the drivers linked into the
  firmware.
- Linux applications may install V4L2, GStreamer, libargus, replay, or other
  drivers.
- Tests explicitly install a replay driver.

There is no automatic fake fallback. If no driver supports a discovery request,
the result is an empty device list. If a selected device fails to open, session
start fails explicitly.

The registry is an actor. It snapshots its short in-memory provider list and
passes that immutable snapshot to a nonisolated discovery operation before any
provider I/O suspends. Provider I/O therefore never runs while synchronously
accessing registry state. Active driver replacement is rejected. Dynamic plugin
loading is not required for Embedded Swift.

The smoke-stage `AVCaptureDeviceRegistry.discoverySession(...)` operation is an
async composition API that returns an immutable
`AVCaptureDevice.DiscoverySession` result. It does not yet claim the synchronous
Apple discovery-session initializer; adding that declaration requires a
separate refresh and snapshot-observation contract.

Embedded Swift uses the driver's synchronous owner-isolated provider contract.
Its registry performs the same explicit registration, filtering, identity, and
typed-failure checks without actor isolation because the owner controls all
access. It dynamically loads no provider and performs no implicit fallback.

## Source and output contract

A selected source is opened once per compatible session topology. The session,
not the driver, owns downstream fan-out.

```text
Source sample
     │ same sequence ID, source timestamp, format revision
     ├── AVCaptureVideoDataOutput
     ├── encoded/file output
     └── Open observation output extension
```

Each output has an independent bounded delivery policy. A slow observation
consumer cannot retain all camera buffers or stall a real-time video consumer.
Frame dropping is observable and includes a typed reason. Buffer leases are
released when every routed consumer has completed or dropped the sample.

The sample-routing path is zero-copy by contract. The registry owns only small
device descriptors and provider references; it never owns, converts, or caches
media bytes. Source payload ownership remains with the driver-provided lease,
and routing must pass borrowed views or lease references without materializing
payloads as `Array`, `Data`, or another copied representation. Any unavoidable
copy belongs at an explicit external or persistence boundary and requires
documented justification plus allocation or copy-count verification.

The observation output is an OpenAVFoundation extension, not an Apple API claim.
It accepts an injected processor and emits structured timed samples. It does not
import Manas or select an inference model.

## Concurrency and lifecycle

Native/WASM Driver-resource transitions use an internal actor because opening,
starting, stopping, and rollback may suspend and order matters. Synchronous graph
state uses a short `Mutex` critical section and performs no Driver I/O while
locked. Embedded uses an owner-isolated synchronous state machine.

```text
idle → configuring → starting → running → stopping → idle
                       │          │
                       └──────────┴──► failed
```

Rules:

1. A session opens sources only after graph validation.
2. Start succeeds only after every required source and output is ready.
3. Partial start failure closes resources already opened.
4. Stop finishes output streams and releases all buffer leases.
5. Runtime driver failure transitions the session to a visible failure state.
6. An `AsyncStream`-based extension exposes explicit shutdown and finishes its
   continuation.
7. No callback or event is emitted while holding a mutex.

## Platform model

The shared compatibility and driver-contract targets depend only on the Swift
standard library, OpenCoreMedia, and OpenCoreVideo.

| Platform | Separately packaged driver | Native payload path |
|---|---|---|
| Browser WASM | MediaDevices / MediaStreamTrack / WebCodecs driver | `VideoFrame`, WebGPU, or linear memory |
| Non-browser WASM | host-import or replay driver | host lease or linear memory |
| Embedded Swift | statically linked board or sensor driver | DMA or fixed-memory lease |
| Linux generic | V4L2 or GStreamer driver | `mmap`, DMA-BUF, or negotiated buffers |
| Jetson | libargus or GStreamer integration | NVMM, `NvBufSurface`, DMA-BUF, CUDA interop |
| Apple test host | replay/reference driver | pure-Swift fixture buffers |

JavaScriptKit, browser globals, Glibc, V4L2, GStreamer, CUDA, libargus, and vendor
camera headers remain outside the shared products.

Platform capability differences are reported through capability queries. A driver
must not pretend to support focus, exposure, format, rotation, or synchronization
controls that the platform cannot perform.

## Error contract

Discovery, authorization, device availability, graph validation, configuration,
open, start, stream, backpressure, interruption, and shutdown failures remain
distinguishable typed failures internally.

Apple-compatible throwing and status APIs translate those failures at the public
boundary. Unsupported controls never become no-ops, and missing devices never
become replay or synthetic devices without explicit application configuration.

## Implementation sequence

1. Record the initial Apple API inventory and signature sources.
2. Define driver descriptor, discovery, open, configuration, stream, and shutdown
   contracts without a concrete driver.
3. Implement a replay driver package and use it to test discovery and lifecycle.
4. Implement `AVMediaType`, device types, positions, formats, and discovery.
5. Implement device input, ports, output, and connection compatibility.
6. Implement atomic session graph configuration.
7. Implement start, sample routing, backpressure, failure, and shutdown.
8. Implement video data output over `CMSampleBuffer` and `CVPixelBuffer`.
9. Implement browser WASM and Embedded reference drivers.
10. Implement Linux/Jetson drivers as separate packages.
11. Add external conformance tests against Apple AVFoundation.

Every stage requires behavior tests and relevant WASM and Embedded builds.
Declaration presence, an import test, or a mock-only path is never completion.
